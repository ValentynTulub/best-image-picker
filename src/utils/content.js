export const HOW_IT_WORKS_POINTS = [
  'Randomized knockout bracket with byes as needed.',
  'Each win adds a point; Top-3 ranked by wins (ties -> total pixels, then name).',
  'Pairs reshuffled each round to avoid repetition from the very start; with few images, repeats later are expected by design.',
]

export const HOW_IT_WORKS = `How it works:\n${HOW_IT_WORKS_POINTS.join('\n')}`
